import { NextResponse } from 'next/server';import { supabaseAdmin } from '@/lib/supabase';
export async function POST(req:Request){const b=await req.json();const {data:match}=await supabaseAdmin.from('matches').select('*').eq('id',b.match_id).single(); if(!match||new Date(match.kickoff_at)<=new Date()) return NextResponse.json({error:'Partido iniciado'},{status:400}); const {error}=await supabaseAdmin.from('predictions').upsert({group_id:b.group_id,participant_id:b.participant_id,match_id:b.match_id,home_goals:b.home_goals,away_goals:b.away_goals}); if(error) return NextResponse.json({error:error.message},{status:400}); return NextResponse.json({ok:true});}
