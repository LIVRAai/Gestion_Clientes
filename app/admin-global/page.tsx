'use client';import { useEffect,useState } from 'react';
export default function Admin(){const [k,setK]=useState('');const [matches,setMatches]=useState<any[]>([]);async function load(){const r=await fetch('/api/leader?type=matches');setMatches((await r.json()).matches||[]);} useEffect(()=>{load();},[]);
return <main className='p-4 max-w-4xl mx-auto'><h1 className='text-2xl font-bold'>Admin global</h1><input className='border p-2 w-full' placeholder='GLOBAL_ADMIN_KEY' value={k} onChange={e=>setK(e.target.value)}/><button className='border px-3 py-2 mt-2' onClick={async()=>{await fetch('/api/matches/sync',{method:'POST',headers:{'x-admin-key':k}});load();}}>Sincronizar football-data.org</button><ul>{matches.map(m=><li key={m.id}>{m.home_team} vs {m.away_team} - {m.status}</li>)}</ul></main>}
